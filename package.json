{
	"name": "ip-lookup-app",
	"version": "1.0.0",
	"description": "",
	"main": "server.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node server.js",
		"build": "cd client && npm install && npm run heroku-postbuild"
	},
	"engines": {
		"node": "14.17.3"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"express": "^4.17.1",
		"node-fetch": "^3.0.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.12"
	}
}
